# This file has been auto-generated by i2-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i2-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
#

# define desktop name 
set $d1 1 www
set $d2 2 term
set $d3 3 code
set $d4 4 mysql
set $d5 5 files
set $d6 6 gtg
set $d7 7 docs
set $d8 8 mail
set $d9 9 skype

# put screens on twin view mode
#exec xrandr --output DVI-I-1 --primary --right-of HDMI-1



#exec xset -dpms
exec --no-startup-id numlockx on
#exec mount -t cifs //perfscript/nas /mnt/nas -o user=nas,password=nas,uid=1000,gid=100,workgroup=WORKGROUP,port=446
#exec i3-msg workspace 3; i3-msg; terminator 
#exec --no-startup-id i3-msg 'workspace 2; exec gedit';
#exec --no-startup-id i3-msg 'workspace 3; exec thunderbird';
#exec --no-startup-id i3-msg 'workspace 2; exec firefox https://wiki.archlinux.org/index.php/i3#New_method:_i3bar'; workspace 3
#exec --no-startup-id i3-msg 'workspace 1 www; exec google-chrome --new-window http://sesame2.0.dev';
exec terminator --command="tmux"
#exec gtg
exec shutter
# exec slack
exec flameshot
#exec screencloud
#exec thunderbird 
#exec skype
#exec dolphin
exec nemo --no-desktop
exec redshift-gtk
exec subl
#exec phpstorm
#exec mysql-workbench
exec smartgit
exec nm-applet
exec xset -dpms
exec google-chrome
#exec davmail ~/.davmail.conselio
exec volumeicon
exec gnome-settings-daemon
#exec i3-msg workspace 2; exec 
exec --no-startup-id "polybar -r main"
# exec --no-startup-id "polybar -r second"

# Redémarre polybar quand on redémarre i3
# exec_always --no-startup-id "killall polybar;polybar -r center & polybar -r left & polybar -r right"
# exec_always --no-startup-id "killall polybar;polybar -r main"

#bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 0 +6.25%
#bindsym XF86AudioLowerVolume exec pactl set-sink-volume 0 -- -6.25%
#bindsym XF86AudioMute exec pactl set-sink-mute 0 -- toggle

#bindsym KP_0 exec gnome-terminal
#bindsym F1 exec google-chrome http://google.fr/; workspace $d1
#bindsym Control+F1 exec /usr/bin/terminator -x vim /mnt/nas/Documents/Documentations/; workspace $d2

set $mod mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# Rofi comme lanceur d'application
bindsym $mod+d exec rofi -show run

#bindsym Ctrl+Alt+l exec terminator
#bindsym Mod1+Ctrl+a exec /usr/bin/keepass2 --auto-type
bindsym $mod+Ctrl+l exec i3lock -i /home/dany/Images/linux.png -n -t
#bindsym $mod+Ctrl+x exec sudo pm-suspend

#bindsym $mod+w exec 
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+m focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+M move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
#bindsym $mod+z layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+d focus child
bindsym $mod+F1 exec --no-startup-id exec google-chrome about:blank; workspace $d1; exec xdotool key --clearmodifiers Return;
bindsym $mod+F2 workspace $d2
bindsym $mod+F3 exec --no-startup-id subl /home/dany/.tmp/tmp.txt; workspace $d3;
bindsym $mod+F5 exec --no-startup-id nemo --no-desktop;

#exec_always --no-startup-id "killall polybar;polybar -r main"
bindsym $mod+F11 exec "/home/dany/.i3/xrandr_single.sh"
bindsym $mod+F12 exec "/home/dany/.i3/xrandr_dual.sh"

assign [class="^Dolphin"] $d5
assign [class="^Nemo"] $d5
assign [class="^Thunderbird"] $d8
assign [class="^Skype"] $d9
assign [class="slack"] $d9
assign [class="^Terminator"] $d2
assign [class="^Subl"] $d3
assign [class="^Brackets"] $d4
assign [class="^Dbeaver"] $d6
assign [class="^SmartGit"] $d7
assign [class="^google-chrome"] $d1
assign [class="^jetbrains-phpstorm"] $d3
assign [class="Gnome-calendar"] $d6
#assign [class="^jetbrains-datagrip"] $d4

# a define default WS on startup
#workspace "1 : www" output DVI-I-1
#workspace "2 : cons" output DVI-I-1
#workspace "3 : csa" output DVI-I-1
#workspace "5 : files" output DVI-I-1
#workspace "6 : mail" output DVI-I-1
#workspace "7 : tmp" output DVI-I-1


# switch to workspace
bindsym $mod+1 workspace $d1
bindsym $mod+2 workspace $d2
bindsym $mod+3 workspace $d3
bindsym $mod+4 workspace $d4
bindsym $mod+5 workspace $d5
bindsym $mod+6 workspace $d6
bindsym $mod+7 workspace $d7
bindsym $mod+8 workspace $d8
bindsym $mod+9 workspace $d9

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $d1
bindsym $mod+Shift+2 move container to workspace $d2
bindsym $mod+Shift+3 move container to workspace $d3
bindsym $mod+Shift+4 move container to workspace $d4
bindsym $mod+Shift+5 move container to workspace $d5
bindsym $mod+Shift+6 move container to workspace $d6
bindsym $mod+Shift+7 move container to workspace $d7
bindsym $mod+Shift+8 move container to workspace $d8
bindsym $mod+Shift+9 move container to workspace $d9

# Move the current workspace to the next output
bindsym $mod+comma move container to output right
bindsym $mod+period move container to output left
bindsym $mod+slash move workspace to output right
bindsym $mod+exclam move workspace to output left
bindsym $mod+Tab workspace back_and_forth



# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+e exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

focus_follows_mouse yes

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#
#   #status_command    i3status
#   output	     eDP-1
#   status_command    $HOME/.i3/bin/conky-i3bar-right
#   position          top
#   workspace_buttons yes
#   tray_output	     eDP-1
#}
#bar {
#   #status_command    i3status
#   output	     DP-1-1
#   status_command    $HOME/.i3/bin/conky-i3bar
#   position          top
#   workspace_buttons yes
#   tray_output	     DP-1-1
#}
#bar {
#   #status_command    i3status
#   output	     DP-2
#   status_command    $HOME/.i3/bin/conky-i3bar
#   position          top
#   workspace_buttons yes
#   tray_output	     DP-2
#}
#bar {
#   status_command    i3status
#   output	     DP-1-3 
#   #status_command    $HOME/.i3/bin/conky-i3bar
#   #status_command    $HOME/.i3/bin/conky-i3bar-right
#   position          top
#   workspace_buttons yes
#   tray_output	     DP-1-3
#}

#bar {
#   status_command    i3status
#   output	     HDMI1
#   status_command    $HOME/.i3/bin/conky-i3bar
#   position          top
#   workspace_buttons yes
#   tray_output	     HDMI1
#}
#tray_output primary

